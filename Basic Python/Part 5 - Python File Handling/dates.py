'''
PYTHON: DATES MODULES

A date in Python is not a data type of its own, but we can import a module named datetime to work with dates as date objects.
'''
# Display the current date:
import datetime

x = datetime.datetime.now()
print(x)
